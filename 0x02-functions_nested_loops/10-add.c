#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @frst: first number input
 * @sec: second number input
 * Return: one + two
 */
int add(int frst, int sec)
{
	return (frst + sec);
}
