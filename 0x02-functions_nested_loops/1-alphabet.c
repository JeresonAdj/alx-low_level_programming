#include "main.h"
/**
 * print_alphabet - Prints all the alphabet in lowercase
 *
 * Return: Always 0 
 */
void print_alphabet(void)
{
	char all;

	for (all = 'a'; all <= 'z'; all++)
	{
		_putchar (alll);
	}
	_putchar('\n');
}
