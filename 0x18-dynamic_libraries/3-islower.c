#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: the character to check characters
 * Return: 1 or c if lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
